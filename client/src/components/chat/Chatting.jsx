import React from "react";

/**
 * Chatting — Legacy component, kept for reference.
 * The active chat UI is now handled by ChatLayout, MessageList, and MessageBubble.
 * This file had several bugs (class= instead of className=, missing asset file).
 */
const Chatting = () => null;

export default Chatting;
